# 📘 PYTHON & IA — GUÍA FINAL (ULTRA RESUMEN)

## 🧠 BASE

Dato→info=dato+contexto | Python: imperativo+POO+funcional

---

## 🔧 FUNCIONES

```python
def f(a): return a
```

✔ función≠método (obj.metodo())

---

## 📦 ESTRUCTURAS

Lista[] mutable | Tupla() inmutable | Dict{key:val} | Set{} sin orden/dup

---

## 🔁 LOOPS

for / while | break=corta | continue=salta | pass=nada

---

## 🧠 STRINGS (INMUTABLE)

.strip() limpia extremos ❌no cambia → reasignar
.split() str→list | " ".join(lista) list→str

---

## ⚡ LIST COMPREHENSION

```python
[exp for x in lista if cond]
```

✔ izq=transforma | der=itera

---

## ⚡ FUNCIONAL

lambda x:x*2
map(f,l) | filter(f,l) | reduce(f,l)
✔ evita loops explícitos

---

## 🧠 ENUMERATE

```python
for i,v in enumerate(l)
```

✔ índice+valor

---

## 🧠 CLASES

```python
class A:
 def __init__(self): self.x=1
```

✔ self=instancia

---

## 🧬 HERENCIA

```python
class Hijo(Padre): super().__init__()
```

✔ reutiliza/extiende

---

## 🔒 ENCAPSULAMIENTO

self._x | get/set
✔ controla acceso

# error: modificar directo sin validar

---

## ⚠ INPUT

✔ int(input()) + try/except
✔ validar rango manual

# error: confiar en tipo sin validar valor

---

## ⚙️ DICCIONARIO

dic[key] | dic.get(k,def)
✔ get evita error

# error: get no valida lógica

---

## 🔁 MAPEO

✔ dict reemplaza múltiples if

---

## 🧠 GENERADORES (yield)

```python
def g(): yield x
```

✔ pausa | guarda estado | next() continúa

```python
next(g)
```

---

## ⚠ ERRORES YIELD

❌ pensar lista
❌ usar print
❌ olvidar consumo
❌ no contar yields

✔ regla: 1 yield = 1 salida

---

## 🧠 ESTADO

✔ variables + punto ejecución
yield guarda | next sigue

# clave: no reinicia

---

## ⚠ ERRORES CLAVE

❌ no reasignar strings
❌ confundir función/método
❌ usar list como nombre
❌ no validar input
❌ confiar solo en get
❌ pensar next reinicia

---

## 🚀 BUENAS PRÁCTICAS

✔ validar siempre
✔ nombres claros
✔ separar lógica
✔ entender flujo antes

---

## 🎯 CLAVE FINAL

Flujo > sintaxis
Estado > resultado
Pensar > codear
